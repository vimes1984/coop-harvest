# المساهمة في تجمع المزارعين ☘️

شكرًا لاهتمامك بالمساهمة في **تجمع المزارعين** (Growers' Collective)! سواء كنت مطورًا، مصممًا، كاتب محتوى، مزارعًا، أو ناشطًا مجتمعيًا، فإن مساعدتك لا تقدر بثمن في بناء شبكة تعاونية أكثر عدلاً ومباشرة من المزارع إلى المستهلك.

> [!IMPORTANT]
> **حالة إثبات المفهوم (PoC)**  
> هذا المشروع حاليًا عبارة عن نموذج أولي للتطوير المحلي وإثبات المفهوم. ليس لدينا مستخدمون نشطون أو معاملات حية بعد. تم تصميم جميع أنماط قاعدة الكود، والسجلات الوهمية، وبيانات التوطين للتحقق من صحة البنية التجريبية لمشروع ريادي حي في المستقبل.

---

## 🗺️ كيف تشارك وتساهم معنا؟

نرحب بالمساهمات بمختلف الأشكال:
* **💻 التطوير والبرمجة**: إصلاح الأخطاء، أو إضافة ميزات إلى الواجهة الأمامية (React) أو الواجهة الخلفية (Express)، أو تحسين التعبئة للمنصات الأصلية (Electron/Capacitor).
* **🎨 تصميم واجهة وتجربة المستخدم (UI/UX)**: تحسين تخطيطات الأجهزة المكتبية والهواتف المحمولة المستجيبة، أو تصميم لوحات تحكم سهلة الاستخدام، أو إنشاء الرسومات البصرية.
* **✍️ كتابة المحتوى والتوطين**: كتابة قصص عن المزارع الأيرلندية المحلية، أو ترجمة المصطلحات، أو تحسين المحتوى التعليمي حول السيادة الغذائية.
* **🚜 التواصل والخدمات اللوجستية**: تصميم قوالب التوزيع المجتمعية (مثل أدلة استلام نوادي GAA، وإرشادات مراكز الأبرشية).

---

## 🛠️ إعداد التطوير المحلي

تم هيكلة المشروع كمستودع أحادي منفصل (Decoupled Monorepo) يحتوي على واجهة أمامية `/frontend` (React + TypeScript) وواجهة خلفية `/backend` (Express + MongoDB).

### المتطلبات الأساسية
* Node.js (يوصى بالإصدار 16.x أو أحدث)
* MongoDB (إما نسخة محلية أو [عنقود سحابي مجاني على MongoDB Atlas](https://www.mongodb.com/cloud/atlas))
* Git

### خطوات إعداد بيئة العمل بالتفصيل

1. **عمل تفريعة (Fork) واستنساخ للمستودع**:
   ```bash
   git clone https://github.com/vimes1984/coop-harvest.git
   cd coop-harvest
   ```

2. **تهيئة الواجهة الخلفية**:
   - انتقل إلى دليل `/backend`.
   - انسخ ملف `.env.example` إلى `.env` (أو أنشئ ملف `.env` جديدًا).
   - قم بإعداد متغير البيئة `MONGO_URI` (على سبيل المثال، سلسلة الاتصال من Atlas) والمنفذ المطلوب `PORT` (الافتراضي هو `5001`).
   - قم بتثبيت الاعتماديات وتشغيل خادم التطوير:
     ```bash
     npm install
     npm run dev
     ```
   - *ملاحظة: إذا كان خادم MongoDB متصلاً وقاعدة البيانات فارغة، سيقوم الخادم تلقائياً بإدخال البيانات الأولية للمزارعين الأيرلنديين، والمنتجات، والمقترحات.*

3. **تهيئة الواجهة الأمامية**:
   - انتقل إلى دليل `/frontend`.
   - قم بتثبيت الاعتماديات:
     ```bash
     npm install --ignore-scripts
     ```
   - قم بتشغيل خادم تطوير Vite:
     ```bash
     npm run dev
     ```
   - افتح متصفحك على الرابط [http://localhost:5173](http://localhost:5173).

---

## 🤝 سير عمل المساهمة

نحن نتبع نموذج التفرع وسحب التغييرات القياسي (Fork and Pull Model):

1. **ابحث عن قضية (Issue)**: تصفح القضايا المفتوحة أو افتح قضية جديدة لمناقشة مقترح ميزة جديدة أو تقرير عن خطأ برمجى.
2. **أنشئ فرعًا جديدًا (Branch)**: أنشئ فرعًا للميزة انطلاقًا من فرع `main` وقم بتسميته بشكل وصفي (مثل `feature/add-csa-calculator` أو `bugfix/fix-checkout-total`).
3. **احفظ تغييراتك (Commit)**:
   - اجعل التغييرات مركزة واكتب رسائل التزام (commit messages) واضحة ووصفية.
   - الحفاظ على حالة قاعدة الكود هذه كـ **إثبات مفهوم (PoC)** أمر بالغ الأهمية. تأكد من أن أي نص مضاف يوضح بوضوح التقسيمات/العمليات على أنها *توقعات/نماذج مستهدفة* لتجنب تضليل المستخدمين.
4. **أرسل طلب سحب (Pull Request - PR)**:
   - قدم ملخصًا واضحًا للتغييرات التي أجريتها وأشر إلى رقم القضية ذات الصلة.
   - تأكد من تجميع الكود الخاص بك بنجاح (تشغيل `npm run build` بدون أخطاء في كل من الواجهتين الأمامية والخلفية).

---

## 🎨 إرشادات الكود والتصميم

للحفاظ على التميز البصري والتقني:
* **الأنماط القوية (Strong Typing)**: استخدم واجهات TypeScript لجميع نماذج البيانات، وخصائص المكونات (props)، واستجابات واجهة برمجة التطبيقات (API). تجنب استخدام نوع البيانات `any`.
* **Vanilla CSS**: نقوم بتنسيق المكونات باستخدام رموز CSS نظيفة ومرنة داخل الملف `/frontend/src/App.css` بدلاً من أطر العمل الممتلئة والمنتفخة. التزم بلوحة ألوان ذات طابع زجاجي مميز باللون الأخضر والترابي الدافئ.
* **سياسة عدم ترك عناصر نائبة (Zero Placeholder Policy)**: لا تترك تعليقات غير مكتملة مثل `// TODO` أو تخطيطات وهمية معطلة. تأكد من وجود حالة احتياطية وهمية تعمل بالكامل إذا كان خادم قاعدة البيانات غير متصل بالإنترنت.

---

## 📜 السلوك التعاوني

بما أن هذا المشروع مدفوع مجتمعيًا ويركز على السيادة الغذائية، والزراعة الإيكولوجية، والحوكمة الديمقراطية، فإننا نثمن الاحترام، والتعاون المفتوح، والشمولية. يرجى دعم الآخرين، والتعاون بشفافية، واحترامه الخلفيات المتنوعة للجميع.

إذا كانت لديك أي أسئلة، فلا تتردد في التواصل معنا عن طريق فتح قضية (Issue) على GitHub!
